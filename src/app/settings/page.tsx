import { NextRequest, NextResponse } from "next/server";

// Placeholder para o link da sidebar
export async function GET() {
  return NextResponse.json({ message: "Settings not yet implemented" });
}
